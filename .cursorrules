# JaminView V2 - AI Development Workflow & Constraints

如果你是 AI 编码助手（如 Cursor, Github Copilot, GPT-4），当处理本库 `jaminview` 下的任何前端开发任务时，你必须且绝对严格地遵守以下三个维度的最高准则！禁止以“我是 AI 语言模型”等理由推脱或忘记，这等同于本仓库的宪法准则。

## RULE 1: 高密度工业级视觉审美法案 (Design Tokens & Aesthetics)

在进行任何左右两侧工具栏、面板、表单的 UI 修改与搭建时：
1. **拒绝默认平庸包裹**：绝对不允许直接生套带过大 Padding 的臃肿卡片结构，一律取消原先泛滥的 `rounded-2xl`，变更为严谨干练的 `rounded-md` 甚至小圆角。
2. **高紧凑度设计语言**：
   - 所有的输入框 `<input>` 和 选择器 `<select>` 的高度必须压缩！如强制使用 `h-7` (28px height)。
   - 文案字号强制使用缩小尺码以适配 IDE：所有表单与提示文本必须使用 `text-xs` 或 `text-[11px]`。
   - 禁止让 Label 文字和输入框在垂直空间上占据两行，能在一行内通过 Flex/Grid 对齐的绝对不允许换行。
3. **深海视觉空间层**：不允许满屏全白。所有的主控面板底色是燕麦灰 `#fafaf5`，交互元素或激活状态必须用内敛深邃的墨绿 `#23422a`；各类线条仅允许存在轻量的过渡线如 `border-[#d7d8d1]/60`。禁止使用 100% 纯黑描边。悬浮必须要用高深阴影视差。

## RULE 2: "恶魔代言人" 防御验证流程 (Devil's Advocate QA Process)

在你根据用户的指示写出或修改完核心代码后、给出最终版代码响应之前，你必须在你自己的推演块里（如 `<thinking>` 或 ````markdown 日志）执行以下三步骤。如果你没做这三步，你的回答会被直接 Reject（拒绝提交）：

1. **破绽推敲 (Vulnerability Hunt)**：如果这是一个拖拽或动画交互，想象用户疯狂地在极端小窗口、或无限多段连续快点、疯狂撤销重做（Undo/Redo）。找出在这个场景下你刚写的代码的 2 个必然会崩坏的崩溃源。
2. **打上防御补丁 (Inject Defense)**：在即将输出的代码中加上数据兜底、防抖（Debounce）、深拷贝断层或 Try-Catch 错误边界，以解决上面找出的这 2 个问题。
3. **输出防呆声明**：在回答结尾，附带一小段：`[ QA 验证预演结论 ]：为了防止由于 XXX 导致的程序死机白屏，我已在代码里附加了 XXX 兜底，已成功规避该问题。`

## RULE 3: 不发明复杂物理轮子协定

如果要做涉及到矩阵转换、2D/3D 画布引擎拖拽和空间映射等功能。绝对不要使用原生 `onMouseMove`、`onMouseUp` 手搓计算。
你只能依赖已有的重量级开源库（例如 `react-rnd`, `react-zoom-pan-pinch`）进行胶水粘合，AI 仅负责通过组件的回调事件（如 `onDragStop` 等）桥接 Zustand 里的数据持久化更新。把晦涩运算外包出去！
